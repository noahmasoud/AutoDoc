/**
 * Re-exports test file
 * Tests re-export scenarios
 */

// Direct export
export class LocalClass {
  public method(): void {
    // Implementation
  }
}

// Re-export from another module (simulated)
// In real scenarios, this would be: export { ExternalClass } from './other-module';
// For testing, we'll just have the structure

